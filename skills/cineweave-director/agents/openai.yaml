interface:
  display_name: "CineWeave Director"
  short_description: "Professional shots from exact character, scene and production bindings"
  default_prompt: "Use $cineweave-director to turn exact CharacterBinding, SceneBinding, InteractionConstraintSet and production-control refs into a coherent proposal, storyboard, image prompt or human-gated RenderPlan."
