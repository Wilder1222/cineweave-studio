interface:
  display_name: "CineWeave Director"
  short_description: "Direct standalone or contract-bound prompts, shots and storyboards"
  default_prompt: "Use $cineweave-director to direct this image or sequence from my brief and any exact character, scene or style contracts I provide."
policy:
  allow_implicit_invocation: true
