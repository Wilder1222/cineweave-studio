interface:
  display_name: "CineWeave Production"
  short_description: "Plan repeatable assets, controls, evidence and validation"
  default_prompt: "Use $cineweave-production to create the smallest production recipe, control plan and validation package for this deliverable."
policy:
  allow_implicit_invocation: true
