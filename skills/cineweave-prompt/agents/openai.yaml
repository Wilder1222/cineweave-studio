interface:
  display_name: "CineWeave Prompt"
  short_description: "Design and manage observable text-to-image prompts"
  default_prompt: "Use $cineweave-prompt to turn my visual intent into a reusable provider-neutral image prompt with scoped references and testable constraints."
policy:
  allow_implicit_invocation: true
